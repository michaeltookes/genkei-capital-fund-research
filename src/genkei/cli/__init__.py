"""Genkei CLI entry point. Phase 3 implementation; this file currently holds a stub."""

from __future__ import annotations

import sys


def main(argv: list[str] | None = None) -> int:
    """Console-script entry point. Will be replaced in Phase 3."""
    print(
        "genkei CLI is not yet implemented. See docs/backlog.md Phase 3 (B-037..B-047).",
        file=sys.stderr,
    )
    return 1
