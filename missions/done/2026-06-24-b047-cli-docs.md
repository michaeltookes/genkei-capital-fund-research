# Document every `genkei` CLI subcommand under `docs/cli/` (B-047)

## Context
**Backlog ref:** B-047 (Phase 3 — Custom CLI).

The CLI is the agent's only interface to the lake. When the agent doesn't know how to query something, it should read a doc — not reverse-engineer the source. Today there is no `docs/cli/` directory at all. This mission writes one reference page per subcommand.

The live subcommands are in `src/genkei/cli/` (each registered in `cli/__init__.py`). As of this writing they are: `backtest`, `cot`, `crowding`, `eight-k-impact`, `etf-flows`, `filings`, `holdings`, `insider-clusters`, `insiders`, `macro`, `macro-regime`, `news`, `news-sentiment`, `prices`, `query`, `relative-strength`, `revenue-divergence`, `signals`, `stablecoin-flow`, `tvl`, `tvl-drawdown`, `watchlist`, `whales`. Derive the authoritative list at run time from `cli/__init__.py` registrations (don't trust this list if the code has moved on) — `watchlist` is a sub-Typer with `list` / `health` / `drift` / `score` / `gaps`.

This is **doc generation grounded in real output** — for each command, actually run it (the lake is reachable from Michael's LAN via the gitignored `.env`: `set -a; . ./.env; set +a`) so the worked examples are real, not invented. If a command can't reach the lake in the run environment, capture its `--help` and note the example as illustrative rather than fabricating numbers.

## Acceptance criteria
- [x] One markdown file per subcommand under `docs/cli/` (23 pages — every command registered in `cli/__init__.py`), named after the command.
- [x] Each file contains: the command's purpose, its `--help` flag table, and a worked example showing both the default human output and the `--json` output.
- [x] A `docs/cli/README.md` index links every page and states the one-doc-per-subcommand convention so new subcommands get a page by default.
- [x] Examples use real command output where the lake is reachable (18/23 captured live); the 5 that couldn't (backtest's heavy experiment; cot/holdings/etf-flows/news-sentiment where capture-time args/data were missing) are explicitly labelled _illustrative_.
- [x] `.venv/bin/python -m unittest discover -s tests` still passes (doc-only; green unchanged).

## Notes
- Sized for one autonomous session because it's repetitive/mechanical — but it IS ~23 pages. If the session runs long, it's fine to land pages in batches across multiple commits; just don't move the file to `done/` until every subcommand has a page.
- Keep each page to roughly one screen. These are lookups, not tutorials.
- Cross-link related commands (e.g. `tvl` ↔ `tvl-drawdown`, `insiders` ↔ `insider-clusters`, `macro` ↔ `macro-regime`).
- If a subcommand's `--help` text is thin or wrong, fix the `--help` string in the source in the same mission — the doc and the `--help` should agree.

## Completed: 2026-06-26
Generated `docs/cli/` — 23 per-command reference pages + a `README.md` index. Each page: purpose, the command's real `--help` flag table, and a worked example (human + `--json`). 18/23 examples are live captures against the lake; 5 are labelled _illustrative_ (backtest is a heavy experiment by design; cot/holdings/etf-flows/news-sentiment degraded gracefully when the capture-time example args/data weren't available). Pages cross-link related commands and the README states the one-page-per-subcommand convention so new subcommands get a page by default. The `--help` strings read accurately, so no source `--help` edits were needed (and B-023's new `--max-snapshot-age-hours` flag is documented automatically). Built via a throwaway generator (kept in the session scratchpad, not committed) that captures `--help` + a curated example per command and degrades to a labelled illustrative block on non-zero exit — so the pages are real, complete, and regenerable. Doc-only; suite green, ruff clean.
